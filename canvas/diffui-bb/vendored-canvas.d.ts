/**
 * Side effects only: importing the module registers Diffui's canvas custom
 * elements on the page. `customElements.whenDefined("diffui-canvas-workspace")`
 * is what callers wait on, so there is nothing to export.
 */
export {};
